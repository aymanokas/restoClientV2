import { StyleSheet } from 'react-native'
import { Colors } from '../../constants'

export default StyleSheet.create({
  root: {
    flexDirection: 'row',
    paddingBottom: 40
  },
  leftSection: {
    width: 250
  },
  rightSection: {
    width: 860,
    marginTop: 50,
    marginLeft: 25
  }
})
